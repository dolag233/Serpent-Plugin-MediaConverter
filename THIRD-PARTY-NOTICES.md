# Third-party notices

This plugin does not bundle FFmpeg. Encoding and probing use the FFmpeg/ffprobe
pair supplied by the Serpent host (`serpent.media.getBinaryPaths()`).

FFmpeg licensing follows the host application's redistribution of FFmpeg.
See the Serpent application's third-party notices for the bundled FFmpeg
build (typically LGPL).
